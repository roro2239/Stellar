jdk:
  - openjdk21
before_install:
  - sdk install java 21.0.2-open
  - sdk use java 21.0.2-open
  - yes | sdkmanager "ndk;29.0.13113456" || true
install:
  - ./gradlew :api:publishToMavenLocal -x :manager:build -x :demo:build -x :server:build
